# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test Database Configuration
# IMPORTANT: Use a separate test database!
DB_HOST=localhost
DB_PORT=5432
DB_NAME=performance_review_test
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration (use different secret for tests)
JWT_SECRET=test-secret-key-for-testing-only-change-in-production
JWT_EXPIRES_IN=2h

# Email Configuration (not used in tests, but required by config)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test_key
AWS_SECRET_ACCESS_KEY=test_secret
EMAIL_FROM=test@example.com

# Logging
LOG_QUERIES=false

# 2FA
TWO_FA_ISSUER=Performance Review System Test
